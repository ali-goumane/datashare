import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { Card } from '../components/Card'; import { Button } from '../components/Button'; import { Input } from '../components/Input'; import { Callout } from '../components/Callout'; import { useAuth } from '../hooks/useAuth'
export function RegisterPage() { const navigate = useNavigate(); const { register } = useAuth(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [confirmation, setConfirmation] = useState(''); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); async function submit(event: React.FormEvent) { event.preventDefault(); if (password !== confirmation) return setError('Les mots de passe ne correspondent pas.'); setError(''); setLoading(true); try { await register(email, password); navigate('/espace') } catch (reason) { setError(reason instanceof Error ? reason.message : 'Inscription impossible') } finally { setLoading(false) } } return <div className="auth-page"><div className="eyebrow">Commencer</div><h1>Créer un compte</h1><p className="intro">Gardez une trace de tous vos partages, simplement.</p><Card><form onSubmit={submit}><Input id="register-email" label="Adresse email" type="email" required autoComplete="email" value={email} onChange={(event) => setEmail(event.target.value)} /><Input id="register-password" label="Mot de passe" type="password" required minLength={8} autoComplete="new-password" value={password} onChange={(event) => setPassword(event.target.value)} /><Input id="register-confirmation" label="Confirmer le mot de passe" type="password" required minLength={8} autoComplete="new-password" value={confirmation} onChange={(event) => setConfirmation(event.target.value)} />{error && <Callout>{error}</Callout>}<Button type="submit" disabled={loading}>{loading ? 'Création…' : 'Créer mon compte'} <span>→</span></Button></form></Card><p className="auth-switch">Déjà inscrit ? <Link to="/login">Se connecter</Link></p></div> }
